/**
 * modularity give us:
 *     maintainability
 *     reusability
 *     Abstraction
 *

IMP:different module format earlier there was not modules, to developer used to use:
  AMD : used in browser
  COMMENT JS : used in node
  UMD : used in node/browser

  es6: natively support modules


TOPIC ES6 tools

Transpiler: take modern js code and  convert that code in js code that all browsers can understand (BABAL)

Module bundler: combining all js file into a single file (WEBPACK) : webpack combine all files into one file, minify code (remove white space and comments ), uglify (shortening the name of variables and function)

*/
